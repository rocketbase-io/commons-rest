language: java
jdk:
  - oraclejdk8
services:
  - mongodb
## gpg stuff
before_install:
  - echo $GPG_SECRET_KEYS | base64 --decode | $GPG_EXECUTABLE --import
  - echo $GPG_OWNERTRUST | base64 --decode | $GPG_EXECUTABLE --import-ownertrust

# code climate configure
before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build
## default build
script: mvn --settings .travis/settings.xml -U clean install
# code climate upload coverage
after_script:
  - JACOCO_SOURCE_PATH=commons-rest/commons-rest-api/src/main/java ./cc-test-reporter format-coverage -d -t jacoco -o coverage/codeclimate.rest-api.json commons-rest/commons-rest-api/target/site/jacoco/jacoco.xml
  - JACOCO_SOURCE_PATH=commons-rest/commons-rest-server/src/main/java ./cc-test-reporter format-coverage -d -t jacoco -o coverage/codeclimate.rest-server.json commons-rest/commons-rest-server/target/site/jacoco/jacoco.xml
  - ./cc-test-reporter sum-coverage -d coverage/codeclimate.*.json -p 2                  # Sum both coverage parts into coverage/codeclimate.json
  - if [[ "$TRAVIS_TEST_RESULT" == 0 ]]; then ./cc-test-reporter upload-coverage -d; fi  # Upload coverage/codeclimate.json

## release on tag
deploy:
  - provider: script
    script: ".travis/deploy.sh"
    skip_cleanup: true
    on:
      tags: true

## disable emailing
notifications:
  email: false